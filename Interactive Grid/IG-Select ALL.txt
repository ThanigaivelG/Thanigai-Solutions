--------- Interactive Grid -> Select All Records --------- 

function(options) {

 options.defaultGridViewOptions = {
     persistSelection: true
  };

  options.modelOptions = {
     pageSize: 10000, 
     fetchAllData: true 
  };

  return options;

 }

------------------------------------------------------------------------------------------------------

// STATIC ID -> Report Static ID

var records = apex.region("STATIC ID").widget().interactiveGrid("getViews","grid").getSelectedRecords() 

console.log(records)

------------------------------------------------------------------------------------------------------

