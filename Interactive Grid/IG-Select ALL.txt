--------- Interactive Grid -> Select All Records --------- 

function(options) {

 options.defaultGridViewOptions = {
     persistSelection: true
  };

  options.modelOptions = {
     pageSize: 10000, 
     fetchAllData: true 
  };

  return options;

 }

------------------------------------------------------------------------------------------------------

var records = apex.region("RG_GRID").widget().interactiveGrid("getViews","grid").getSelectedRecords() 

console.log(records)

------------------------------------------------------------------------------------------------------
